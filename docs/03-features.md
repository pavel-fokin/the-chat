# Features

A feature is something a person can use that does a job listed in
`01-product.md`. Infrastructure is not a feature. No job, no entry.

## Feature Index

None yet. The first lands with the first vertical slice.

The server currently serves the built frontend, which is plumbing — no job in
`01-product.md` is "load a page". An empty index here is the accurate reading of
the product surface, and it stays empty until something clears the bar above.
